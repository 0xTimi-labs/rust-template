# 代码审查规范

本文件供所有审查者（人类或 AI）使用。审查时按以下优先级给出发现，并标注严重程度。

## 审查优先级

1. **正确性**：逻辑错误、边界条件、并发问题、错误处理遗漏
2. **安全性**：注入风险、密钥处理、不安全依赖、越权访问
3. **可测试性**：新代码是否被测试覆盖、测试断言是否空洞（只调用不断言的测试视为没测）
4. **可维护性**：命名清晰度、重复代码、不必要的复杂度
5. **风格**：格式问题交给 clippy/fmt，不要人肉提

## 产出格式

- 按 **P0（必须修）/ P1（应该修）/ P2（建议）** 分级列出发现
- 每条发现附具体文件和行号
- 最后给出明确 verdict：approve / request changes

## 本项目特别约定

- 全部代码由 AI 辅助开发：重点警惕幻觉 API（调用了不存在的方法）、过时语法、以及"看起来对但语义错"的实现
- 测试是第一公民：修复 bug 必须附带复现该 bug 的测试
- 公开 API 变更需人工确认是否构成 semver breaking change；启用 cargo-semver-checks（taiki-e/install-action 一行接入）后可自动化把关
